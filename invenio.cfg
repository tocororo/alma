"""
InvenioRDM settings for Alma project.

This file was automatically generated by 'invenio-cli init'.

For the full list of settings and their values, see
https://inveniordm.docs.cern.ch/reference/configuration/.
"""

from datetime import datetime
from invenio_i18n import lazy_gettext as _


def _(x):  # needed to avoid start time failure with lazy strings
    return x

# Flask
# =====
# See https://flask.palletsprojects.com/en/1.1.x/config/

# Define the value of the cache control header `max-age` returned by the server when serving
# public files. Files will be cached by the browser for the provided number of seconds.
# See flask documentation for more information:
# https://flask.palletsprojects.com/en/2.1.x/config/#SEND_FILE_MAX_AGE_DEFAULT
SEND_FILE_MAX_AGE_DEFAULT = 300

# SECURITY WARNING: keep the secret key used in production secret!
# Do not commit it to a source code repository.
# TODO: Set
SECRET_KEY="CHANGE_ME"

# Since HAProxy and Nginx route all requests no matter the host header
# provided, the allowed hosts variable is set to localhost. In production it
# should be set to the correct host and it is strongly recommended to only
# route correct hosts to the application.
APP_ALLOWED_HOSTS = ['0.0.0.0', 'localhost', '127.0.0.1','10.2.6.168','alma.upr.edu.cu']


# Flask-SQLAlchemy
# ================
# See https://flask-sqlalchemy.palletsprojects.com/en/2.x/config/

# TODO: Set
SQLALCHEMY_DATABASE_URI="postgresql+psycopg2://alma:alma@localhost/alma"


# Invenio-App
# ===========
# See https://invenio-app.readthedocs.io/en/latest/configuration.html

APP_DEFAULT_SECURE_HEADERS = {
    'content_security_policy': {
        'default-src': [
            "'self'",
            'data:', # for fonts
            "'unsafe-inline'",  # for inline scripts and styles
            "blob:",            # for pdf preview
            # Add your own policies here (e.g. analytics)
        ],
    },
    'content_security_policy_report_only': False,
    'content_security_policy_report_uri': None,
    'force_file_save': False,
    'force_https': True,
    'force_https_permanent': False,
    'frame_options': 'sameorigin',
    'frame_options_allow_from': None,
    'session_cookie_http_only': True,
    'session_cookie_secure': True,
    'strict_transport_security': True,
    'strict_transport_security_include_subdomains': True,
    'strict_transport_security_max_age': 31556926,  # One year in seconds
    'strict_transport_security_preload': False,
}


# Flask-Babel
# ===========
# See https://python-babel.github.io/flask-babel/#configuration

# Default locale (language)
BABEL_DEFAULT_LOCALE = 'es'
# Default time zone
BABEL_DEFAULT_TIMEZONE = 'Europe/Zurich'


# Invenio-I18N
# ============
# See https://invenio-i18n.readthedocs.io/en/latest/configuration.html

# Other supported languages (do not include BABEL_DEFAULT_LOCALE in list).
I18N_LANGUAGES = [
    # ('de', _('German')),
    # ('tr', _('Turkish')),
]


# Invenio-Theme
# =============
# See https://invenio-theme.readthedocs.io/en/latest/configuration.html

# Name used in header and UI
THEME_SITENAME = "Alma"
# Frontpage title
THEME_FRONTPAGE_TITLE = 'Repositorio de la Universidad de Pinar del Río "Hermanos Saíz Montes de Oca"'
# Header logo
THEME_LOGO = 'images/logo-alma-large.svg'


# Invenio-App-RDM
# ===============
# See https://github.com/inveniosoftware/invenio-app-rdm/blob/master/invenio_app_rdm/config.py

# Instance's theme entrypoint file. Path relative to the ``assets/`` folder.
INSTANCE_THEME_FILE = './less/theme.less'

# Email address for administrator emails (like file checksum alerts)
APP_RDM_ADMIN_EMAIL_RECIPIENT = "rafael.martinez@upr.edu.cu"

# Default values for the deposit form
APP_RDM_DEPOSIT_FORM_DEFAULTS = {
    "publication_date": lambda: datetime.now().strftime("%Y-%m-%d"),
    "rights": [
        {
            "id": "cc-by-4.0",
            "title": "Creative Commons Attribution 4.0 International",
            "description": ("The Creative Commons Attribution license allows "
                            "re-distribution and re-use of a licensed work "
                            "on the condition that the creator is "
                            "appropriately credited."),
            "link": "https://creativecommons.org/licenses/by/4.0/legalcode",
        }
    ],
    "publisher": "Alma",
}

APP_RDM_DEPOSIT_FORM_AUTOCOMPLETE_NAMES = 'search' # "search_only" or "off"

# Invenio-Records-Resources
# =========================
# See https://github.com/inveniosoftware/invenio-records-resources/blob/master/invenio_records_resources/config.py

# TODO: Set with your own hostname when deploying to production
SITE_UI_URL = "https://alma.upr.edu.cu"

SITE_API_URL = "https://alma.upr.edu.cu/api"

# Invenio-RDM-Records
# ===================
# See https://inveniordm.docs.cern.ch/customize/dois/
DATACITE_ENABLED = False
DATACITE_USERNAME = ""
DATACITE_PASSWORD = ""
DATACITE_PREFIX = ""
DATACITE_TEST_MODE = True
DATACITE_DATACENTER_SYMBOL = ""

# Authentication - Invenio-Accounts and Invenio-OAuthclient
# =========================================================
# See: https://inveniordm.docs.cern.ch/customize/authentication/

# Invenio-Accounts
# ----------------
# See https://github.com/inveniosoftware/invenio-accounts/blob/master/invenio_accounts/config.py
ACCOUNTS_LOCAL_LOGIN_ENABLED = True  # enable local login
SECURITY_REGISTERABLE = True  # local login: allow users to register
SECURITY_RECOVERABLE = True  # local login: allow users to reset the password
SECURITY_CHANGEABLE = True  # local login: allow users to change psw
SECURITY_CONFIRMABLE = True  # local login: users can confirm e-mail address
SECURITY_LOGIN_WITHOUT_CONFIRMATION = False # require users to confirm email before being able to login

# Invenio-OAuthclient
# -------------------
# See https://github.com/inveniosoftware/invenio-oauthclient/blob/master/invenio_oauthclient/config.py

OAUTHCLIENT_REMOTE_APPS = {}  # configure external login providers

from invenio_oauthclient.views.client import auto_redirect_login
ACCOUNTS_LOGIN_VIEW_FUNCTION = auto_redirect_login  # autoredirect to external login if enabled
OAUTHCLIENT_AUTO_REDIRECT_TO_EXTERNAL_LOGIN = False  # autoredirect to external login

# Invenio-UserProfiles
# --------------------
USERPROFILES_READ_ONLY = False  # allow users to change profile info (name, email, etc...)

# OAI-PMH
# =======
# See https://github.com/inveniosoftware/invenio-oaiserver/blob/master/invenio_oaiserver/config.py

OAISERVER_ID_PREFIX = "alma.upr.edu.cu"
"""The prefix that will be applied to the generated OAI-PMH ids."""
OAISERVER_ADMIN_EMAILS = [
    "rafael.martinez@upr.edu.cu",
]

# Invenio-Search
# --------------

SEARCH_INDEX_PREFIX = "alma-"

# Invenio-Users-Resources
# -----------------------

USERS_RESOURCES_ADMINISTRATION_ENABLED = True
"""Enable the user administration"""

THEME_SHOW_FRONTPAGE_INTRO_SECTION = False

RDM_ALLOW_METADATA_ONLY_RECORDS = False

COMMUNITIES_ADMINISTRATION_DISABLED = False



# Enable community requirement
RDM_COMMUNITY_REQUIRED_TO_PUBLISH = False
from alma.permissions import CustomCommunitiesPermissionPolicy
# Apply custom permissions
COMMUNITIES_PERMISSION_POLICY = CustomCommunitiesPermissionPolicy
COMMUNITY_CREATOR_ROLE = "community-creator"  # Customizable role name


from invenio_i18n import lazy_gettext as _

from invenio_rdm_records.contrib.codemeta import (
    CODEMETA_CUSTOM_FIELDS,
    CODEMETA_CUSTOM_FIELDS_UI,
    CODEMETA_FACETS,
    CODEMETA_NAMESPACE,
)
from invenio_rdm_records.contrib.imprint import (
    IMPRINT_CUSTOM_FIELDS,
    IMPRINT_CUSTOM_FIELDS_UI,
    IMPRINT_NAMESPACE,
)
from invenio_rdm_records.contrib.journal import (
    JOURNAL_CUSTOM_FIELDS,
    JOURNAL_CUSTOM_FIELDS_UI,
    JOURNAL_NAMESPACE,
)
from invenio_rdm_records.contrib.thesis import (
    THESIS_CUSTOM_FIELDS,
    THESIS_CUSTOM_FIELDS_UI,
    THESIS_NAMESPACE,
)
from invenio_rdm_records.contrib.meeting import (
    MEETING_CUSTOM_FIELDS,
    MEETING_CUSTOM_FIELDS_UI,
    MEETING_NAMESPACE,
)

from invenio_rdm_records.config import RDM_FACETS, RDM_SEARCH
from invenio_records_resources.services.records.facets import CFTermsFacet

from alma.custom_fields import (
    UPR_CUSTOM_FIELDS,
    UPR_CUSTOM_FIELDS_UI,
    UPR_CUSTOM_FACETS,
    UPR_NAMESPACE,
)

RDM_NAMESPACES = {
    **CODEMETA_NAMESPACE,
    **JOURNAL_NAMESPACE,
    **IMPRINT_NAMESPACE,
    **THESIS_NAMESPACE,
    **MEETING_NAMESPACE,
    # **UPR_NAMESPACE,
}

RDM_CUSTOM_FIELDS = [
    *CODEMETA_CUSTOM_FIELDS,
    *MEETING_CUSTOM_FIELDS,
    *JOURNAL_CUSTOM_FIELDS,
    *IMPRINT_CUSTOM_FIELDS,
    *THESIS_CUSTOM_FIELDS,
    # *UPR_CUSTOM_FIELDS,
]

RDM_CUSTOM_FIELDS_UI = [
    {
        "section": _("Publishing information"),
        # "hide_from_landing_page": True,
        "fields": [
            # journal
            *JOURNAL_CUSTOM_FIELDS_UI["fields"],
            # imprint
            *IMPRINT_CUSTOM_FIELDS_UI["fields"],
            # thesis
            *THESIS_CUSTOM_FIELDS_UI["fields"],
        ],
    },
    # meeting
    MEETING_CUSTOM_FIELDS_UI,
    CODEMETA_CUSTOM_FIELDS_UI,
    # UPR_CUSTOM_FIELDS_UI,
]

# MEETING_CUSTOM_FIELDS_UI["hide_from_landing_page"] = True
# CODEMETA_CUSTOM_FIELDS_UI["hide_from_landing_page"] = True

# RDM_FACETS = {
#     **RDM_FACETS,
#     **CODEMETA_FACETS,
#     **UPR_CUSTOM_FACETS,
# }

# RDM_SEARCH = {
#     **RDM_SEARCH,
#     "facets": RDM_SEARCH["facets"] + ["facultades"]
# }
#
#

ACCOUNTS_LOCAL_LOGIN_ENABLED = True

USERPROFILES = True
USERPROFILES_READ_ONLY = True

SECURITY_REGISTERABLE = False  # deny local registration of new users
SECURITY_RECOVERABLE = False  # deny resetting the local account's password
SECURITY_CHANGEABLE = False  # deny changing passwords for local accounts


# Dex OAuth settings
# OAUTHCLIENT_REMOTE_APPS = {
#     'dex': {
#         'title': 'UPR Login',
#         'description': 'Login via University LDAP',
#         'icon': 'fa fa-university',
#         'params': {
#             'base_url': 'https://idp.upr.edu.cu/',
#             'request_token_params': {'scope': 'openid email profile'},
#             'access_token_url': 'https://idp.upr.edu.cu/token',
#             'authorize_url': 'https://idp.upr.edu.cu/auth',
#             'access_token_method': 'POST',
#             'app_key': 'OAUTHCLIENT_DEX_CREDENTIALS',
#         }
#     }
# }

# DEX_OPENID_CONFIG = dict(
#     title="Dex OpenID",
#     description="Connecting to Dex OpenID", 
#     icon= "fa fa-university",
#     base_url='http://127.0.0.1:5556',
#     credentials = dict(
#         consumer_key='localhost-alma',  # Client ID from Dex
#         consumer_secret='your_strong_secret_here',  # Secret from Dex
#     )
# )

# OAUTHCLIENT_REMOTE_APPS = {
#     'dex': {
#         'title': 'UPR Login',
#         'description': 'Login via University LDAP',
#         'icon': 'fa fa-university',
#         'params': {
#             'base_url': 'http://127.0.0.1:5556/',
#             'request_token_params': {'scope': 'openid email profile'},
#             'access_token_url': 'http://127.0.0.1:5556/token',
#             'authorize_url': 'http://127.0.0.1:5556/auth',
#             'access_token_method': 'POST',
#             'app_key': 'OAUTHCLIENT_DEX_CREDENTIALS',
#         }
#     }
# }

ACCOUNTS_USERNAME_REGEX = r"^[a-zA-Z][a-zA-Z0-9-]+(?:\.[a-zA-Z0-9-]+)*$"
ACCOUNTS_USERNAME_RULES_TEXT = _(
    "Username must start with a letter, be at least three characters long and"
    " only contain alphanumeric characters, dot, dashes and underscores."
)

from alma.registration_form import alma_registration_form

# use the custom form function
OAUTHCLIENT_SIGNUP_FORM = alma_registration_form


# Client credentials (match Dex staticClients config)
DEX_APP_CREDENTIALS = {

    'consumer_key': 'localhost-alma',  # Client ID from Dex
    'consumer_secret': 'your_strong_secret_here',  # Secret from Dex
}


from alma import dex

upr = dex.DexOAuthSettingsHelper(
    title="Identidades UPR",
    description="Proveedor de identidades de la UPR", 
    base_url='https://idp.upr.edu.cu',
    # base_url='http://127.0.0.1:5556',
)

BASE_APP = upr.base_app
REMOTE_APP = upr.remote_app

OAUTHCLIENT_REMOTE_APPS = dict(
    dex= upr.remote_app,
)
